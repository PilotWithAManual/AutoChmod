# AutoChmod
**AutoChmod** — A tiny interactive bash tool that makes `.sh` scripts executable in bulk (or one-by-one) via a simple terminal menu, with `[+] / [!] / [-]` status output and optional directory handling.
